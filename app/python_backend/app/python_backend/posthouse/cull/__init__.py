"""posthouse.cull — the Assistant Editor's technical cull (Phase 4).

Package home for the six build slices in
``docs/design/PHASE4_CULL_DESIGN.md`` §5. This is slice 1 only
(:mod:`posthouse.cull.signals`, the deterministic signal extractor); no
classification, segmentation, fitting, or ``culls.json`` writer exists
here yet. See ``docs/contracts/CULLS.md`` for the eventual output
contract and the module docstring of :mod:`posthouse.cull.signals` for
what this slice actually does.
"""
